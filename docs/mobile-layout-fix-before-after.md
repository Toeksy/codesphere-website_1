# Business Card Mobile Layout Fix – Before/After

## Ongelma (ENNEN)

```
┌─────────────────────────────────────────────┐
│  📱 MOBILE (<720px)                         │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ .card (min-height: 100dvh - 16px)    │ │
│  │                                       │ │
│  │  .inner (grid-rows: auto auto)       │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │ .left (min-height: 300px)       │ │ │
│  │  │                                 │ │ │
│  │  │  ┌──────────────────────────┐  │ │ │
│  │  │  │ .browser (height: 100%)  │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  │  [SELAIN]                │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  └──────────────────────────┘  │ │ │
│  │  │                                 │ │ │
│  │  │  ⬅ Tyhjää tilaa (ei veny!)      │ │ │
│  │  │                                 │ │ │
│  │  └─────────────────────────────────┘ │ │
│  │                                       │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │ .right (auto korkeus)           │ │ │
│  │  │                                 │ │ │
│  │  │  [YHTEYSTIEDOT]                 │ │ │
│  │  │                                 │ │ │
│  │  └─────────────────────────────────┘ │ │
│  │                                       │ │
│  │  ⬅ Tyhjää tilaa (grid ei veny)       │ │
│  │                                       │ │
│  └───────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘

❌ Ongelma:
- .inner käyttää grid-template-rows: auto auto
- .left min-height: 300px, mutta ei veny täyteen
- .browser height: 100%, mutta parentin korkeus on vain 300px
- Paljon tyhjää tilaa kortin ylä- ja alapuolella
```

## Ratkaisu (JÄLKEEN)

```
┌─────────────────────────────────────────────┐
│  📱 MOBILE (<720px)                         │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ .card (min-height: 100dvh - 16px)    │ │
│  │                                       │ │
│  │  .inner (grid-rows: 1fr 1fr)         │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │ .left (flex column, min-h: 0)   │ │ │
│  │  │                                 │ │ │
│  │  │  ┌──────────────────────────┐  │ │ │
│  │  │  │ .browser (flex: 1)       │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  │  [SELAIN]                │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  │  ↕ Venyy täyteen!         │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  │                          │  │ │ │
│  │  │  └──────────────────────────┘  │ │ │
│  │  │                                 │ │ │
│  │  └─────────────────────────────────┘ │ │
│  │                                       │ │
│  │  ┌─────────────────────────────────┐ │ │
│  │  │ .right (1fr korkeus)            │ │ │
│  │  │                                 │ │ │
│  │  │  [YHTEYSTIEDOT]                 │ │ │
│  │  │                                 │ │ │
│  │  └─────────────────────────────────┘ │ │
│  │                                       │ │
│  └───────────────────────────────────────┘ │
│                                             │
└─────────────────────────────────────────────┘

✅ Korjattu:
- .inner käyttää grid-template-rows: 1fr 1fr
- .left on flex-container (display: flex, flex-direction: column)
- .browser käyttää flex: 1 → venyy täyttämään koko tilan
- Ei tyhjää tilaa – selain hyödyntää koko korkeuden
```

## CSS-muutokset

### ENNEN:
```css
@media (max-width: 720px) {
  .inner {
    grid-template-columns: 1fr;
    grid-template-rows: auto auto;  /* ❌ Ei veny */
    gap: 14px;
    inset: 12px;
  }
  
  .left { 
    min-height: 300px;  /* ❌ Kiinteä korkeus */
  }
  
  .browser {
    /* Ei flex-säännöstä → ei veny */
  }
}
```

### JÄLKEEN:
```css
@media (max-width: 720px) {
  .inner {
    grid-template-columns: 1fr;
    grid-template-rows: 1fr 1fr;  /* ✅ Jakaa tilan tasaisesti */
    gap: 14px;
    inset: 12px;
  }
  
  .left { 
    min-height: 0;  /* ✅ Flex-container voi venyä */
    display: flex;
    flex-direction: column;
  }
  
  .browser {
    flex: 1;  /* ✅ Venyy täyttämään tilan */
    min-height: 0;  /* ✅ Estää overflow */
  }
}
```

## Testaus

### DevTools (Chrome/Edge):
```
F12 → Toggle device toolbar (Ctrl+Shift+M)

📱 iPhone SE (375 × 667):
   - ENNEN: Selain ~300px korkea, paljon tyhjää tilaa
   - JÄLKEEN: Selain ~350px korkea, täyttää tilan

📱 iPad (768 × 1024):
   - ENNEN: Selain ~220px korkea (tablet media query)
   - JÄLKEEN: Selain ~500px korkea, venyy täyteen

💻 Desktop (1024 × 768):
   - Ei muutoksia (toimii edelleen kuten ennen)
```

### UI Smoke Test:
```powershell
pwsh -NoProfile -ExecutionPolicy Bypass -File .\tools\run_ui_smoke.ps1

# Output:
UI SMOKE: PASS ✅
```

## Yhteenveto

| Ennen | Jälkeen |
|-------|---------|
| Grid-rows: `auto auto` | Grid-rows: `1fr 1fr` |
| `.left { min-height: 300px }` | `.left { display: flex; flex-direction: column; min-height: 0; }` |
| `.browser` ei veny | `.browser { flex: 1; min-height: 0; }` |
| Paljon tyhjää tilaa | Täysi tilan hyödyntäminen |
| ❌ Sommittelu "kasassa" | ✅ Sommittelu venyttää koko alueen |

**Lopputulos:** Selain hyödyntää nyt koko vasemman alueen korkeuden mobiilissa ja tabletilla. 🎉
